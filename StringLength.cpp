#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    // Prompt the user (helpful for interactive runs)
    cout << "Enter a string: ";
    if (!getline(cin, s)) return 0;
    cout << "Length: " << s.length() << '\n';
    return 0;
}
